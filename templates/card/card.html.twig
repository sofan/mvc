{% extends "base.html.twig" %}

{% block title %}Card game
{% endblock %}

{% block main %}
<h2>Cards startsida</h2>
<div class="deckContainer">
    <div class="cardAside">
        <a class="button" href="{{ path('card_deck_sort') }}">Visa kortlek</a>
        <a class="button" href="{{ path('card_deck_shuffled') }}">Blanda</a>
        <a class="button"  href="{{ path('card_deck_draw') }}">Dra ett kort</a>
        <a class="button" href="{{ path('card_deck_init') }}">Ny kortlek</a>
        <a class="button" href="{{ path('session_info') }}">Session</a>
    </div>

        <form action="{{ path('card_hand_init') }}" method="POST">
			<div class="form-group">
				<label for="players">Antal spelare:</label>
				<input type="number" id="players" name="players" min="1" value="2" max="5" required>
			</div>

			<div class="form-group">
				<label for="cards">Antal kort:</label>
				<input type="number" id="cards" name="cards" min="1" value="5" max="10" required>
			</div>
			<button type="submit">Dela ut kort</button>
		</form>

</div>
<div class='uml'>
    <h3>Beskrivning av klasserna</h3>
    <ul>
        <li><code>Card:</code> klass som beskriver ett kort i kortleken</li>
        <li><code>GraphicCard:</code> klass som ärver från Card och överrider funktionen getAsString() för att presentera kortet grafiskt.</li>
        <li><code>DeckOfCards:</code> representerar en kortlek och har relationen aggregat till Card. Card kan finnas även om inte DeckOfCard finns.</li>
        <li><code>CardHand:</code> representerar en korthand och har relationen aggregat till Card.</li>
    </ul>
    <img src="{{ asset('img/Card_uml.webp') }}" alt="Card UML" title="Card UML">
</div>




{% endblock %}
